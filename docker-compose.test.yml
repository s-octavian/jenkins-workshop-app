version: '3.8'
services:
  app:
    build: .
    ports:
      - "3000" # Expose port for the test runner

  tests:
    build:
      context: .
      dockerfile: Dockerfile.test
    depends_on:
      - app
    environment:
      - APP_URL=http://app:3000
